<h4>Enter a friend's GitHub handle</h4>

<%= form_tag('/invite/create') do %>
  <p><%= text_field_tag "GitHub Handle" %></p>
  <%= submit_tag "Send Invite", class: "btn btn-primary mb1 bg-teal" %>
<% end %>
